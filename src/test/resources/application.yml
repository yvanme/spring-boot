spring:
  # for H2 embedded database only
  datasource:
    url: jdbc:h2:mem:promotion;DB_CLOSE_DELAY=-1
    username: sa

  jpa:
    database: mysql
    show-sql: true
    open-in-view: true
    hibernate:
      ddl-auto: update





